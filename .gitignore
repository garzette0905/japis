node_modules/
dist/

# wrangler / worker
.wrangler/
.dev.vars

# Claude Code 로컬 설정 (개인/머신 전용)
.claude/

# 로컬 환경변수(시크릿 포함) — 절대 커밋 금지. 예시는 .dev.vars.example 참고
.env
*.env
!.env.example
!*.env.example

# 기획 원본 PDF는 docs/ 에 요약만 남기고 원본은 커밋하지 않는다(개인 문서).
*.pdf

# 아이콘 후보 원본은 로컬에만 두고 커밋하지 않는다(용량/중간 산출물).
design/

# 삼성헬스 '개인 데이터 다운로드' 원본(수면·심박 기록). 개인 건강 기록이라 절대 커밋하지 않는다.
# 화면의 '가져오기' 또는 tools/samsung-health-import.mjs 가 여기서 읽어 하루 한 값만 D1 에 올린다.
Samsung Health/
samsunghealth_*/

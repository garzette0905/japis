# JAPIS 로드맵

## 반영 완료 (2026-09-20)

### 헬스정보 · 인바디·혈액 — 검진 결과지를 **비교할 수 있는 모양**으로

`healthcheck` 자리는 오래 '준비중'이었다. 채웠다. 다만 채운 것이 무엇인지 분명히
적어 둔다 — **사진을 모으는 기능이 아니다.**

집에 있는 10개년 검진 책을 그대로 찍어 올려 둔들, 책장에 꽂아 둔 것과 다를 게
없다. 검색도 안 되고 추세도 안 보인다. 이 기능의 값은 **연도·병원마다 다르게
적힌 검사항목을 코드 하나로 통일하는 일**에 있다. 그래서 표를 셋으로 갈랐다.

| 표 | 무엇 |
|---|---|
| `health_exams` | 검진·측정 한 번 (날짜 · 기관 · 종류) |
| `health_metrics` | **검사항목 이름표 170줄** — 이름·단위·참고범위·좋은 방향 |
| `health_results` | 실제 값 (`exam_id` × `code`) |

값 쪽에는 `code` 만 적는다. 'ALT'·'SGPT'·'혈청지피티'가 해마다 섞여 적혀도 `alt`
하나로 모이고, 지단백(a)처럼 단위가 바뀐 항목(2022년 mg/dL → 2023년 nmol/L)은
코드를 둘로 나눠 둔다. **참고범위는 값에도 둔다** — 비어 있으면 이름표의 것을
쓰고, 적혀 있으면 그것이 이긴다. 이름표에만 두면 2015년 값을 2026년 기준으로
그린 그래프가 나온다.

분류는 **결과지가 묶어 놓은 차례 그대로** 열여섯이다(신체계측 → 체성분 → 혈압·
심장 → … → 스트레스·건강나이). 우리 식으로 다시 묶으면 종이와 화면을 번갈아 볼
때마다 같은 항목을 두 군데서 찾게 된다.

### 책 한 권으로 3개년이 들어왔다

강북삼성병원 결과지는 항목마다 `참고범위 | 2024-09-10 | 2023-09-05 | 2022-09-06`
네 칸을 나란히 적어 준다. **2024년 한 권을 찍는 것으로 세 해가 들어왔다** —
2022·2023년 책을 다시 찍을 필요가 없다(2021년 책을 찍으면 2019~2021이 같은
방식으로 따라 들어온다). 첫 자료 437줄이 `migrations/011` 에 들어 있다.

그리고 **30~40페이지를 다 찍을 필요가 없다.** 책의 대부분은 설명문과 그림이고,
수치가 담긴 표는 5~10페이지다. 이 차이가 10개년을 완주하느냐 마느냐를 가른다.

### 화면 하나, 탭 셋

처음에는 '인바디·혈액'을 옆 메뉴로 따로 뽑았다. **걷었다** — 메뉴에서 나란히 설
이유가 없다. 셋 다 내 몸의 같은 기록이고, 보는 사람은 "건강 얘기"를 하러 한 번
들어온다. 들어와서 탭으로 고르는 것이 맞다.

- **종합검진**(`#/health`) — 표가 **결과지와 같은 모양**이다
  (`항목 | 참고범위 | 이번 | 지난번 | 그 전`). 범위를 벗어난 항목만 모은
  *지켜볼 항목*을 맨 위에 세운다 — 백오십 줄에서 그 열세 줄을 스스로 찾아내게
  하지 않는다. 항목 이름을 누르면 그 항목만 전 기간 선 하나로 뜬다.
- **인바디**(`#/health/inbody`) · **혈액**(`#/health/blood`) — 검진과 검진 **사이**.
  왼쪽에서 항목을 고르고 오른쪽에서 길게 본다. 추이 선에는 **종합검진에서 잰 값도
  같이 올라온다** — 검진에서 잰 혈당과 수시로 잰 혈당은 같은 선 위에 있어야 한다.

탭은 단추가 아니라 **링크**다. 주소가 갈려야 뒤로 가기가 살고, 그 탭을 눌러 둔 채
새로고침해도 같은 자리에 선다. 옛 주소 `#/bodylab` 은 `#/health/inbody` 로 보낸다.

### 검사실마다 기준이 다르다 — 그게 설계의 값을 증명했다

같은 요산 **3.3** 이 강북삼성(2.8~8.2)에서는 정상이고 GC Labs(3.4~7.0)에서는
**낮음(L)** 이다. 결과지에 찍힌 L 과 화면의 판정이 어긋나면 그 화면은 그날로
못 믿을 것이 된다. 그래서:

- 값에 그 회차의 참고범위를 같이 담는다(010 부터 그렇게 설계했다).
- **추이 선의 띠는 가장 최근 회차가 쓴 기준으로 깐다.** 이름표의 범위 하나로
  깔면, 띠 안에 있는 점이 '낮음'으로 찍히는 모순이 화면에 그대로 뜬다.
- 회차마다 기준이 달랐으면 표에 *그때 기준* 칸을 한 줄 더 세우고 그 사실을 적는다.
  말없이 하나만 보여 주면 옛 값의 판정이 틀린 것처럼 읽힌다.

같은 이유로 **eGFR 은 계산식별로 코드를 나눴다**(MDRD · CKD-EPI). 한 선에 이으면
2024년 95 → 2025년 76 이라는, 있지도 않은 급락이 그려진다.

### 인바디 용지에 있는 것을 다 받는다

010 의 이름표는 검진 결과지에 실린 **요약 넷**(근육량·체지방량·체지방률·복부지방률)
만 알고 있었다. 실제 InBody 용지에는 세포내/외수분, 골무기질, 체세포량, 기초대사량,
상완둘레, **부위별 근육 다섯 군데**(팔·몸통·다리 좌우)까지 찍혀 나온다. 그것을 다
받는다 — 좌우가 벌어지는지는 부위별 값에서만 보인다(지금은 팔 88% · 다리 100%).

### '혈액'이 가리키는 것을 좁혔다

010 에서는 피검사로 잴 수 있는 것을 전부 blood 로 묶었다(CBC·전해질·종양표지자까지
예순 개). 그런데 실제로 주기적으로 받는 결과지는 **간·신장·지질·혈당 스무 항목짜리
한 장**이다. 값 입력 폼에 예순 칸을 늘어놓으면 스무 칸 채우려고 예순 칸을 지난다.
그 한 장에 실리는 항목만 권하고, 나머지 175개는 폼 아래 **'펼치기'** 로 내렸다 —
접어 두되 감추지는 않는다(검사실마다 한두 항목이 더 붙어 나온다).

### 2025년 — 병원이 바뀌었다

2025년 검진은 강북삼성이 아니라 **SCL 하나로리더스헬스케어**다. 결과지의 생김새가
달라진 것은 문제가 아니었다(코드로 모으면 그만이다). 값이 있었던 것은 **참고범위가
통째로 다르다**는 사실이다 — ALT 강북삼성 `~35` · SCL `1~40`, 요산 `2.8~8.2` ·
`3.4~7.0`. 행마다 참고범위를 담아 둔 설계가 여기서 제값을 했다.

그리고 여기서 규칙 하나가 더 나왔다. **좋은 쪽으로 벗어난 것은 지켜볼 항목에
올리지 않는다.** 2025년 HDL 61 이 그 검사실 기준(40~60)을 넘어 결과지에 ▲ 가
찍혔다. 화면도 '높음'이라 적는다 — 결과지와 어긋나면 안 되니까. 다만 HDL 은
**높아서 좋은 것**이라, 정작 봐야 할 LDL 옆에 세워 두면 목록의 무게가 깎인다.
판정은 결과지 그대로, 모아 세우는 것은 나쁜 쪽으로 벗어난 것만
(`cloudflare/health.js` 의 `isConcerning`).

### 기기가 바뀌면 코드를 나눈다 — 두 번째 사례

2025-09-02 체성분은 InBody 가 아니라 **ACCUNIQ X-SCAN** 이다. 대부분의 항목은
같은 것을 재므로 같은 코드에 담았다(기기 차이는 `provider` 에 남는다). 딱 하나,
**골격근은 정의 자체가 다르다** — InBody 는 30kg 대, ACCUNIQ 는 20kg 대로 나온다.
한 선에 이으면 근육이 3분의 1 날아간 그래프가 그려진다. eGFR 을 MDRD·CKD-EPI 로
나눈 것과 같은 이유로 코드를 갈랐다.

### 가로축을 달력으로 — 띄엄띄엄 받은 검사가 거짓말하지 않도록

점을 고르게 벌려 놓고 있었다. 1년에 한 번씩 꼬박꼬박 받았다면 그래도 됐겠지만,
실제로는 **2017~2021년은 한 해에 한 번, 2024년 말부터는 두세 달에 한 번**이다.
고르게 벌리면 3년의 공백과 2개월의 간격이 화면에서 같은 너비를 차지해 **선의
기울기가 거짓말을 한다** — 3년에 걸쳐 천천히 오른 것이 두 달 만에 치솟은 것처럼
보인다.

그래서 가로축을 **연 단위 달력**으로 깔았다. 해마다 칸 하나, 점은 그 칸 안의 제
날짜에 앉는다. 기울기가 곧 '얼마나 빨리 움직였나'가 된다. 값 이름표는 자리가 나는
것만 적는다(마지막 점은 언제나) — 몰려 있는 구간에서 다 적으면 숫자가 서로를 덮는다.

### 2021년 책 한 권에서 네 해 — 그리고 그래프에서 한 해 더

2021년 강북삼성 결과지는 비교 열이 **다섯 칸**이다(2021·2020·2019·2018 두 번).
책 한 권으로 네 번의 검진이 들어왔다. 앞쪽 '연도별 주요건강 지표' 그래프에는
**2017-07** 까지 그려져 있어 거기서 열 항목을 더 옮겼다 — 같은 그래프의 2018년
값을 값 표와 맞춰 보니 열 항목이 모두 일치해서 믿을 만하다고 보았다. 다만 다른
점에 가려 읽히지 않은 항목은 **적지 않았다**. 어림한 숫자를 넣느니 비워 둔다.

2018년은 회차가 둘이다. 7월 검진에서 공복혈당 108·당화혈색소 6.2 가 나와 **석 달 뒤
재검**을 받았고, 100·5.7 로 내려왔다. 한 해에 한 점만 찍어서는 보이지 않는 움직임이다.

### 앱 줄 세우기와 아이콘 — 제 마크를 제 색으로

- **wepic 을 홈페이지 묶음으로 내렸다.** 개인서비스는 '내가 쓰는 도구'이고 wepic 은
  주소를 아는 사람이면 누구나 여는 바깥 사이트다. 줄리·SNS 와 같은 칸이 맞다.
- **제 홈페이지가 쓰는 마크를 그대로 가져왔다** — 가계부(보라 막대) · Taylor
  Bookshelf(초록 책장) · 타임라인(파랑 핀)은 각자의 favicon 을 그대로 옮겨 그렸고,
  줄리영어학원은 그 마크 파일을 함께 둔다.
- **구글 넷과 OneDrive 는 공식 제품 마크**를 쓴다. 이 다섯만은 손으로 다시 그리지
  않았다 — 눈이 찾는 것은 '구글 메일'이라는 글자가 아니라 그 빨간 봉투이고, 조금만
  어긋나면 곧바로 가짜처럼 보인다. 그림 파일은 `web/public/brand/` 에 함께 둔다.
  남의 CDN 을 걸어 두면 그쪽이 주소를 바꾸는 날 아이콘이 통째로 사라진다.
- **북마크는 진한 금색 리본.** 폭을 줄이고 아래 홈을 깊게 파서 22px 에서도 리본으로
  읽히게 했다. 옆 기둥에서 제 색으로 서는 단색 그림은 헬스정보(빨강)와 이 줄 둘뿐이다.
- **마크가 칸에 꽉 차게 고쳤다.** `.ico` 가 20px 고정이라 22px·26px 칸 안에서 저 혼자
  작게 앉아 있었다. 선 그림은 티가 안 났지만 제 판을 가진 마크는 어긋난 테두리가
  그대로 보였다 — 상단 띠의 JAPIS 마크가 흐릿해 보이던 것이 이것이었다.

### 지금 들어 있는 것 — 27회차 · 1,618개 값 · 2017년부터

| 종류 | 회차 | 기간 |
|---|---|---|
| 종합검진 | 10 | 2017-07 ~ 2025-09 (2018은 검진과 재검 둘) |
| 혈액검사 | 5 | 2023-03 ~ 2025-11 (헌혈 결과지 1회 포함) |
| 인바디 | 12 | 2021-09 ~ 2026-04 |

LDL 이 **열세 점**으로 이어진다 — 8년 반이 한 선 위에 있다
(147 → 147 → 126 → 141 → 165 → 171 → 149 → 162 → 163 → 148 → 126 → 136 → 152).
검진만 보면 아홉 점, 피검사만 보면 네 점이던 것이 그렇다. 이것이 표를 하나로 둔
이유의 전부다.

### 옆 기둥에서 빨갛게 서는 줄 하나

이 결에서 아이콘은 회색 한 벌이다. 줄이 고르게 서는 값이 있어서다. 그 중 **헬스정보
한 줄만** 제 색(빨강)으로 세웠다 — 색이 흔해지면 그 효과가 곧바로 사라지므로 더
늘리지 않는다.

선을 그릴 때 값만 이어 놓으면 오르내림은 보여도 **그것이 괜찮은 수치인지**는
보이지 않는다. 그래서 참고범위를 연한 띠로 먼저 깔고 선을 그 위에 얹는다 — 띠를
벗어난 점만 빨갛게 찍는다. 화살표는 *수치의 방향*, 색은 *몸에 좋은 방향*이다.
둘은 자주 어긋난다(콜레스테롤 ↓ 은 초록).

### 진단하지 않는다

참고범위 대비 위치(높음·낮음)와 전보다 어느 쪽으로 움직였는지까지만 적는다.
숫자가 없거나 참고범위가 없으면 **아무 말도 하지 않는다** — 모르는 것을 '정상'
이라고 적으면 화면의 정상 개수가 거짓이 된다(`cloudflare/health.test.js`).
화면 아래에는 해석과 판단을 진료로 받으라는 한 줄을 늘 적어 둔다.

### 남은 것

- 2016년 이전 검진 책 — 2021년 책이 2017까지 덮었으므로 그 앞이 남았다
- 결과지 **원본 사진**의 R2 보관(지금은 값만 있고 사진은 OneDrive 에 있다)
- 사진 → JSON 일괄 추출(`tools/`)과 원본 옆에 두고 확인하는 검증 화면
- 삼성헬스 내보내기(ZIP)의 **일별 집계** 임포트 — 연 1회 '점'과 매일의 '선' 겹쳐 보기
- 인바디 용지의 **임피던스**(주파수별 Z) — 지금은 받지 않는다. 추이로 볼 값이 아니다

## 반영 완료 (2026-09-18)

### 안드로이드 위젯 — **안 된다.** 그 대신 바로가기

먼저 안 되는 것부터 적는다. **PWA 를 설치하는 것만으로는 안드로이드 홈 화면 위젯을
만들 수 없다.** 날씨·캘린더처럼 홈 화면에서 내용이 바뀌는 그 위젯은 안드로이드 앱만
만들 수 있다(`AppWidgetProvider` — 코틀린 코드와 스토어에 올리는 앱 껍데기가 필요하다).
크롬·안드로이드에는 웹앱이 부를 수 있는 위젯 API 가 없다.

표준에 `manifest.widgets` 가 있기는 하다. 그것은 **윈도우 11 위젯 보드** 전용이고
서비스 워커와 어댑티브 카드 템플릿이 함께 있어야 돈다 — 안드로이드는 읽지도 않는다.
안드로이드를 위해 그것을 적어 두는 것은 안 되는 것을 된다고 적는 일이라 넣지 않았다.

설치한 웹앱이 홈 화면에 세울 수 있는 것은 **바로가기**까지다. 그 자리를 채웠다.

- `manifest.json` 에 `shortcuts` 네 개 — 북마크 · Jaden wiki · Play Lists · 대시보드.
  앱 아이콘을 길게 누르면 뜨고, 그 한 줄을 **끌어다 홈 화면에 놓으면** 그 화면으로
  바로 들어가는 아이콘이 선다.
- 바로가기마다 96×96 아이콘을 한 벌로 그렸다(`shortcut-*.png`). 런처가 아이콘을
  동그랗게 잘라내므로 판은 모서리를 깎지 않고 그림은 가운데 70% 안에 둔다.
- `launch_handler: navigate-existing` — 이것이 없으면 앱이 이미 열려 있을 때
  바로가기를 눌러도 그 창에 **초점만 오고** 화면은 그대로 있다(해시 주소라 더 그렇다).
- 같은 안내를 포털 안 **내 계정 → 홈 화면 · 위젯** 칸에 두었다. 지금 이 창이 설치된
  앱인지 브라우저 탭인지도 그 자리에서 알려준다 — 설치하지 않은 사람에게 "아이콘을
  길게 누르세요"라고만 적어 두면 누를 아이콘이 없다.

### 북마크 — 첫 화면을 목록에 내주고, 담기는 팝업으로

담는 줄을 목록 바로 위에 붙여 둔 것은 주소 하나 넣으려고 화면을 두 번 옮기지 않게
하려던 것이었다. 그런데 셈이 맞지 않았다 — 북마크는 **담는 일보다 찾는 일이 열 배
잦다.** 한 번 담은 주소를 그 뒤로 몇 달 동안 누른다. 그런데 늘 쓰는 목록 위에 여섯
칸짜리 입력 폼이 상주하면서, 첫 화면에서 목록은 폼 아래로 밀려나 있었다(휴대폰에서는
아예 한 화면에 들어오지도 않았다).

- **첫 화면 = 검색 줄 + 목록.** 열자마자 찾던 것이 보인다.
- **담기·고치기는 팝업 하나**가 맡는다(검색 줄 오른쪽의 *담기*). 둘의 칸이 똑같으므로
  갈라 두면 같은 폼을 두 벌 고쳐야 한다. 한 줄 고치기(인라인 폼)는 걷었다.
- **묻는 창은 전부 `<dialog>`** 로 통일했다. `window.prompt/confirm` 은 PWA 로 설치해
  홈 화면에서 띄운 창이나 앱 안 브라우저(카카오·슬랙)에서 **조용히 무시된다** —
  폴더 이름을 물어봤다고 생각했는데 아무 일도 일어나지 않는다(wiki.js 가 같은 이유로
  먼저 그렇게 했다).
- 담을 때는 지금 보고 있는 칸(사내·사외·폴더)을 미리 골라 둔다. 열어 둔 폴더에
  담으려고 팝업에서 또 고르게 하지 않는다.

> 북마크가 **어디서 들어와도 같은 목록**인 것은 처음부터 그랬다 — 한 줄도
> localStorage 에 두지 않고 전부 D1(`bookmarks` · `bookmark_folders`)에 있다. 이
> 기기에 적어 두는 것은 '사내·사외 중 어디를 보고 있었는지' 하나뿐이다(자료가 아니라
> 보던 자리). 운영 D1 에 `migrations/006_bookmarks.sql` 을 아직 돌리지 않았다면
> 그것부터 한 번 돌린다.

### 아이콘 — 글씨를 줄이고, 글씨에도 그라디에이션

- **글씨를 조금 줄였다.** 512칸 안에서 J 의 키가 402(78%) → **346(68%)**. 판을 가득
  채운 글씨는 작은 칸에서 판을 밀어내는 것처럼 보이고, 안드로이드·iOS 가 아이콘을
  다시 잘라 낼 때(maskable) 글씨가 잘린다.
- **글씨에도 그라디에이션을 넣었다.** 판은 왼위(`#2450f2`) → 오른아래(`#061a97`),
  글씨는 오른위(흰색) → 왼아래(`#b3cdfa`) — 서로 반대 방향으로 흘러야 글씨가 판에서
  떠 보인다. 상단 띠의 마크(`BRAND_ICONS.japis`)만 납작한 `#fff` 여서 앱 아이콘과
  다른 그림으로 보였는데, 이제 같다.
- 글씨의 그라디에이션은 `gradientUnits="userSpaceOnUse"` 로 **J 하나를 통째로** 흐른다.
  기본값으로 두면 가로대·기둥·고리가 저마다 흰색에서 시작해 색이 토막토막 끊긴다.
- png 를 **굽는 스크립트를 저장소에 들였다**(`tools/make-icons.py`). 네 장(+바로가기
  넷)을 손으로 맞추다 보면 한 장을 고치고 나머지를 잊는데, 그것을 눈으로 알아채는 데는
  몇 주가 걸린다. 설치할 것은 없다 — 표준 라이브러리의 zlib 만 쓴다(빌드 단계를
  늘리지 않는 규칙 그대로다).
- 오른쪽 기둥의 키만 svg 와 어긋나 보인다 — 252(고리가 시작하는 306)가 아니라 **254**.
  두 덩어리가 맞닿기만 하면 그리는 쪽이 양쪽 테두리를 따로 부드럽게 칠해 그 줄에
  **가느다란 실금**이 남는다. 두 칸 겹쳐 두면 사라진다(svg·png·`BRAND_ICONS` 셋 다).

## 반영 완료 (2026-09-17, 두 번째)

### '오늘' 세 칸이 어긋나던 **진짜** 이유 — 고침

앞서 안쪽 목록의 높이를 못박아 세 칸의 **크기**는 같아졌는데, 화면에서는 여전히
줄이 맞지 않았다. 원인은 높이가 아니라 **시작하는 줄**이었다 —
`.panel + .panel { margin-top: 16px }`. 판을 위아래로 쌓을 때를 위해 오래전부터
있던 규칙인데, 격자 안에서는 둘째·셋째 칸만 16px 아래로 밀어냈다. 첫 칸만 위에
붙어 있으니 크기가 같아도 어긋나 보인다. 격자 안에서는 그 여백을 걷었다
(`.today > .today-panel { margin: 0 }`). 세 칸의 위와 아래가 같은 줄에 선다.

### 칸 머리에 새로 고침 — 완료

↗(새 탭) 옆에 ↻ 를 두었다. 서버는 받아 온 것을 5분 담아 두는데(KV), 이 단추는
그것을 버리고 지금 것을 다시 묻는다(`/api/feed/<key>?fresh=1`). 방금 처리한 메일이
5분 동안 그대로 남아 있으면 그 목록은 믿을 수 없게 된다. 도는 동안 단추가 돈다 —
아무 일도 안 일어나는 것처럼 보이지 않게.

### 공식 마크를 쓴다 — 완료

SNS 목록에서 눈이 찾는 것은 '인스타그램'이라는 **글자**가 아니라 그 동그란
**마크**다. 우리가 다시 그린 카메라보다 그 회사가 쓰는 그림이 백 배 빨리 읽힌다.
그래서 색을 품은 한 벌(`BRAND_ICONS`)을 예외로 들였다 — 인스타그램·Threads·
Facebook·Telegram·X·LinkedIn·다모앙, 그리고 wepic·카드모아(각자의 favicon 에서
그대로 가져왔다). 우리 마크(JAPIS)도 같은 자리에 넣어 **상단 띠의 'JAPIS' 앞**과
로그인 화면의 간판 위에 세웠다.

획만 있는 마크는 검은 알약 위에서 사라지므로, 전부 **판**(둥근 사각) 모양으로 통일했다.

### 차례를 바꿨다

- 개인서비스 — **Jaden wiki** 가 맨 앞, 그 아래 북마크, 맨 끝에 Play Lists.
- 홈페이지 — Julie · SNS · Word Writer.
- SNS 에 **X**(x.com)를 더했다.

### 네이버 메모 — 뺐다

네이버가 메모 API를 열지 않아 링크 한 줄 이상이 될 수 없었고, 그 자리는 Jaden wiki 가
대신한다. 남은 권한 행은 `007_playlists.sql` 이 함께 지운다.

### Play Lists — 완료

개인서비스 맨 아래에 화면 하나를 세웠다(`#/playlists`). 왼쪽에 가수, 가운데에 담는
줄과 곡 목록 — 북마크와 같은 얼개다(같은 얼개를 두 번 배우게 하지 않는다).

- **가수를 먼저 세운다.** 곡 줄에 이름을 적어 두면 표는 하나로 끝나지만, 이름을
  고쳐 적는 순간 같은 가수가 둘이 된다("아이유" / "IU").
- 그렇다고 **미리 만들 필요는 없다.** 담는 줄에 없는 이름을 적으면 그 자리에서
  가수가 생긴다 — 한 곡 적으려고 두 번 일하게 하지 않는다.
- **들을 주소는 곡마다 선택이다.** 적어 두면 '듣기'가 서고, 안 적어도 목록은
  목록대로 산다. 이 화면의 본업은 재생이 아니라 **적어 두기**다.

표 둘을 더했다 → `migrations/007_playlists.sql` (**배포 전에 먼저 돌린다**).

## 반영 완료 (2026-09-17)

### 이름 — 'Automated' 를 뺀다

**JA**den **P**ersonal **I**ntelligence **S**ervice. 자동화는 수단이지 간판이 아니다 —
홈페이지·매니페스트·바닥글에 적히던 한 줄에서 그 낱말을 걷어냈다.

### 아이콘 — 파란 판 위의 J — 완료

검정 한 장에 'JA' 두 글자를 얹어 두던 표를 **그림 하나**로 바꿨다. 파란 라운드 사각,
흰 J, 고리 안의 구슬. 벡터(favicon.svg)와 픽셀(192·512·apple-touch)을 **같은 좌표로**
구워 두어 어디에 서든 같은 그림이 뜬다. PNG 는 외부 라이브러리 없이 zlib 만으로
직접 구웠다(빌드 단계를 늘리지 않으려고).
(→ 2026-09-18 에 글씨를 줄이고 글씨에도 그라디에이션을 넣었다. 굽는 일은
`tools/make-icons.py` 가 맡는다.)

### 공유한 메모 링크에 로고가 뜬다 — 고침

메모 주소를 카카오톡·슬랙에 붙여 넣으면 **흰 종이 한 장**이 떴다. 미리보기 카드
(Open Graph)에 아무것도 적어 두지 않아서다. 제목·첫 줄·JAPIS 로고(1200×630)를
붙였고, 공유 화면 머리띠에도 같은 마크를 세웠다. 로고는 로그인 없이 열리는 정적
자산이다 — 메신저의 수집기는 우리 세션을 갖고 있지 않으므로 그래야만 뜬다.
`noindex` 는 그대로 둔다(검색에 걸리지 않는 것과 링크 카드가 뜨는 것은 다르다).

### '오늘' 세 칸의 키를 맞춘다 — 고침

PC에서 메일·할일·일정 칸의 높이가 제각각이었다. 격자의 `align-items: stretch` 에만
맡겨 두었더니 **항목이 적은 칸은 제목만 남기고 주저앉았다.** 안쪽 목록의 높이를
다섯 줄로 못박았다(min=max). 서버는 열 개를 실어 보내므로 여섯째부터는 그 칸 안에서
굴러간다 — 늘어나는 것은 격자에 맡기고, 줄어들지 않는 것은 CSS 가 못을 박는다.

### 한 줄 입력칸 — 말로 넣고, 말로 찾는다 — 완료

대시보드 맨 위에 칸 하나를 두었다(`POST /api/ask`).

| 적는 말 | 하는 일 |
|---|---|
| `내일 오후 3시 치과` | 구글 캘린더에 한 시간짜리 일정을 넣는다 |
| `장보기 할일 추가` | 구글 할일에 넣는다 |
| `이번주 일정 뭐 있어?` | 그 주의 일정·할일을 그 자리에서 편다 |
| `메일 청구서` | 받은편지함을 그 낱말로 훑는다 |

**모형(LLM)을 부르지 않는다.** 다루는 말이 좁아서다 — 언제·무엇·넣을까 찾을까 셋뿐이고,
그 셋은 규칙으로 읽는 편이 빠르고 싸고 **틀려도 왜 틀렸는지 보인다**. 대신 읽은 결과를
반드시 되돌려 말한다("9월 18일(금) 15:00 — 치과"). 잘못 읽었으면 그 자리에서
'할일로 다시 넣기'를 누른다.

> ⚠️ 구글 권한이 **쓰기까지** 늘었다(`calendar.events` · `tasks`). 이미 연결해 둔
> 계정의 옛 토큰에는 그 권한이 없다 — 한 번 '연결 끊기 → 연결하기'를 해야 한다
> (화면이 그렇게 안내한다).

### 북마크 — 사내·사외 주소록 — 완료

브라우저 즐겨찾기는 기기마다 따로 논다. 회사 컴퓨터가 집 컴퓨터를 모르고, 휴대폰은
둘 다 모른다. Jaden wiki 아래 줄에 화면 하나를 더 세웠다(`#/bookmarks`).

- **사내와 사외를 먼저 가른다.** 사내 주소는 회사 망 안에서만 열린다 — 밖에서 눌러
  봐야 시간만 버리므로 한 목록에 섞지 않는다.
- **폴더 안에 폴더가 하나 더** 들어간다. 두 겹부터는 "어디 넣었더라"가 시작되고,
  한 겹도 없으면 스무 개가 넘는 순간 목록이 무너진다.
- **담는 줄과 목록이 한 화면**에 있다. 주소 하나 넣으려고 화면을 옮기지 않는다.
  (→ 2026-09-18 에 뒤집었다. 위의 '첫 화면을 목록에 내주고, 담기는 팝업으로' 참고)
- 주소는 들어올 때 한 번 본다 — `http(s)` 가 아니면 받지 않는다(`javascript:` 한 줄이
  목록에 앉으면 그 목록을 여는 사람이 위험해진다).

표 둘을 더했다 → `migrations/006_bookmarks.sql` (**배포 전에 먼저 돌린다**).

### SNS 두 곳 추가 — 완료

LinkedIn 과 다모앙을 SNS 묶음에 더했다. 그림도 한 벌로 그려 넣었다(icons.js).


## 반영 완료 (2026-09-16)

### 사라진 '공유 켜기' 단추 — 고침

공유 창에 단추가 아예 없는 것처럼 보였다. 있기는 있었다 — **흰 글씨가 흰 바탕 위에**
있었을 뿐이다. 옛 규칙 `.wiki-folder-dialog-actions button` 이 클래스+태그라
`.btn-primary`(클래스 하나)보다 힘이 세서 바탕만 흰색으로 덮였고, 글자색은
`.btn-primary` 가 준 흰색 그대로 남았다. 같은 힘으로 맞받아 칠했다 — 창 안의 단추도
포털의 다른 단추와 같은 알약이 되었다.

### 아이콘을 속이 찬 그림으로 — 완료

가는 선(1.75) 그림은 줄은 세웠지만 힘이 없었다. 16px 짜리 메뉴에서 선 그림은 흐릿한
얼룩처럼 보이고, 검은 알약 위에 얹으면 더 그렇다. **속을 채운 덩어리**로 다시 그렸다.
굵고 단순한 덩어리 하나가 가느다란 윤곽선 여럿보다 멀리서도 먼저 읽힌다. 사진의 해,
카메라의 렌즈, 달력의 날짜칸은 선을 더 긋는 대신 덩어리에서 구멍을 팠다(evenodd).
탭에 서는 표(favicon)도 같은 결로 — 검정 한 장 위에 흰 글씨 하나.

### 폴더를 끌어서 옮긴다 — 완료

손이 닿으면 줄 왼쪽에 손잡이가 나온다. 잡고 끌면 차례가 바뀌고 손을 떼는 순간
저장된다(`PUT /api/wiki/folders/order`). **줄 아무 데나 잡게 하지 않는다** — 그러면
"폴더를 열려고 눌렀는데 끌려 나왔다"가 된다. 여는 자리와 옮기는 자리는 달라야 한다.

HTML5 의 `draggable` 을 쓰지 않았다. 그것은 마우스에만 있는 기능이라 휴대폰에서
손가락으로는 아무 일도 일어나지 않는다. Pointer Event 로 직접 만들어 마우스·손가락·
펜을 한 벌로 다룬다. 좁은 화면에서 폴더가 가로로 흐르는 알약이 되는 것까지 셈에
넣었다 — y 만 보면 휴대폰에서 엉뚱한 자리에 끼워진다. 끌어다 놓기를 못 쓰는 사람을
위해 손잡이 위에서 `↑` · `↓` 로도 옮길 수 있게 했다.

### 폴더 목록만 굴러간다 — 고침

기둥이 내용만큼 길어져서, 폴더가 열 개쯤 되면 아래의 '휴지통'에 닿으려고 메모 목록
끝까지 페이지를 내려야 했다. 기둥을 화면 높이에 묶어 두고 그 안에서 **가운데(폴더)만**
굴린다 — '메모 쓰기'는 늘 위에, '휴지통'은 늘 아래에 붙어 있다. 좁은 화면에서는
기둥이 메모 위에 얹히고 페이지가 통째로 굴러가므로 이 묶음을 푼다.

## 반영 완료 (2026-09-15)

### 나이키 결로 갈아입기 — 완료

색을 셋으로 줄였다. 검정 `#111111` · 흰색 · 회색 `#f5f5f5` 가 화면의 95%를 덮고,
동작(단추·활성표시)에 쓰던 파랑 `#0075de` 는 없앴다 — 이제 전부 검정이다. 빨강은
*지운다/위험*, 초록은 *연결됨* 에만 선다. 그림자를 전부 걷고(1px 실선 하나가 유일한
깊이다), 모서리를 둘로 갈랐다: **담는 것은 각지게 0, 누르는 것은 알약.**
글꼴은 `Inter` + `Noto Sans KR`, 간판 글씨(로고)만 `Bebas Neue`.

**아이콘을 이모지에서 선 그림으로.** 📓 ✉️ 📅 는 기기마다 다른 그림이 뜨고 저마다 색과
광택이 달라 나란히 세우면 줄이 서지 않았다. 굵기 하나(1.75)·크기 하나(24)·색은 언제나
`currentColor` 인 한 벌을 새로 그렸다(`web/public/icons.js`). 색을 품지 않으므로 검은
알약 안에서도 같은 그림이 그대로 뒤집힌다.

### 열어 둔 화면이 본문을 덮는다 — 완료

예전에는 화면을 열면 **오른쪽 칸**이 생겨 기둥·본문·화면 셋이 한 줄에 섰다. 셋 중
어느 것도 제 폭을 갖지 못한다 — 특히 메모는 글을 읽고 쓰는 자리라 좁아지는 순간
쓸모가 반으로 준다. 이제 자리를 나눠 갖지 않고 **덮는다.** 기둥은 그대로 남고, 덮개가
열려 있는 동안 본문은 아예 그리지 않는다(뒤에 살아 있으면 스크롤이 두 겹이 된다).
다른 줄로 넘어가면 덮개가 먼저 걷힌다 — 뒤에서 화면만 바뀌고 눈앞이 그대로인 일이
없게.

### '오늘' — 메일 · 할일 · 일정 — 완료

순서를 **메일 → 할일 → 일정**으로 바꿨다(아침에 먼저 보는 것부터 왼쪽). 세 칸은 같은
키로 선다 — 안에 든 항목 수가 달라도 바닥이 한 줄로 맞아야 눈이 세 번 다시 자리를
잡지 않는다. 각 칸에 **열 개**가 들어오고(`FEED_MAX` 5 → 10), 열 줄이 굴리지 않고
그대로 보이는 높이로 잡았다.

**한 줄이 곧 그 항목으로 가는 문이다.** 메일은 그 대화(`threadId`), 일정은 그 일정
(`htmlLink`), 할 일은 그 할 일(`webViewLink`)로 바로 나간다 — 서버가 항목마다 주소를
실어 보낸다. 주소가 없는 항목은 그 화면 자체를 여는 단추가 된다. 눌리지 않는 줄은 없다.

### Jaden wiki — 세 얼굴 · 공유 · 마크다운 — 완료

**편집 · HTML · 보기.** HTML 칸에서는 태그를 직접 적고, 보기에서는 저장했을 때 남들이
보게 될 그 모습을 그대로 본다. HTML 칸에서 나올 때 적은 것을 한 번 씻어서 편집칸에
옮긴다 — 저장할 때 서버가 어차피 다시 씻지만, 씻지 않은 것이 편집칸에 들어가면 그
순간부터 이 페이지 안에서 살아 있는 것이 된다. `<img onerror=…>` 는 저장을 기다려
주지 않는다. 그래서 화면 쪽에도 같은 흰 목록을 한 벌 두었다(`safeHtml` — 브라우저에는
진짜 파서가 있어 정규식 대신 문서를 만들어 걸러낸다).

**공유.** 메모 하나를 주소 하나로 내놓는다(`/s/<이름표>`). 주소를 아는 사람은 로그인
없이 그 메모 한 장만 본다. **주소에 메모 번호를 넣지 않는다** — `/s/3` 이라면 `/s/4` 를
눌러 남의 메모로 건너갈 수 있다. 대신 128비트(32자 hex) 무작위 이름표를 뽑는다.
끄면 이름표를 지우므로 돌던 주소는 그 자리에서 죽고, 다시 켜면 새 이름표가 나온다.
휴지통에 든 메모는 열리지 않는다.

**.md 를 모양 그대로.** `##` · `|---|` 가 글자로 남으면 읽을 수가 없다. 들여오는 그
자리에서 html 로 바꾼다(`mdToHtml`) — 표는 표로, 그림은 그림으로, 체크리스트는
편집기가 쓰는 것과 같은 네모칸으로. 변환기는 서버 한 곳에만 두고, 편집기가 `.md` 를
열 때도 `/api/wiki/render` 로 보내 받는다(두 벌을 두면 곧 어긋난다).

> `migrations/005_wiki_share.sql` 을 운영 D1에 적용해야 공유가 켜진다.

## 반영 완료 (2026-09-13)

### 사진 크기 조절 · 휴대폰 — 완료

**사진 크기** — 사진을 한 번 누르면 고른 상태가 된다. 네 귀퉁이에 손잡이가 붙고 위에
*작게(25%) · 중간(50%) · 크게(100%) · 원본* 단추가 뜬다. 손잡이만 두지 않은 이유는
휴대폰에서 귀퉁이를 정확히 집어 끄는 것이 생각보다 어렵기 때문이다(손가락이 사진을
가린다). 손잡이는 마우스가 있는 곳에서, 단추는 어디서나.

크기는 `<img width="320">` 으로 적는다. style 이 아니라 속성으로 두어야 html 로
내보낸 파일에서도, 본문을 씻어 내는 쪽(`ALLOWED_ATTR.img`)에서도 그대로 살아남는다.
높이는 적지 않는다 — 폭만 주면 브라우저가 비율을 지킨다.

> 여기서도 한 번 밟았다. 손잡이 네 개 중 **왼쪽 둘이 먹지 않았다.** 끌기를 시작할 때
> 부르는 `setPointerCapture` 가 붙잡을 포인터가 없으면 예외를 던지는데, 그 예외가
> 끌기 자체를 시작도 못 한 채 조용히 죽이고 있었다. 이제 끄는 동안의 이벤트를
> **window** 에서 듣는다 — 손이 손잡이 밖으로 나가도 이어지고, 붙잡기에 실패해도
> 상관없다. 그 김에 하나에만 기대던 자리가 없어졌다.

**휴대폰** — 읽기만이 아니라 쓰고 고치는 것까지 되게 했다. 좁은 화면에서 걸리는
세 가지에 각각 답을 두었다.

1. 마우스가 없으면 `hover` 로 숨겨 둔 단추는 **영영 안 나온다.** 카드의 ★·🗑️,
   폴더의 `⋯` 를 `@media (hover: none)` 에서 늘 보이게 하고, 크기 손잡이는 14px →
   20px 로 키웠다.
2. 서식 단추 25개가 줄바꿈하면 다섯 줄이 되어 **정작 글 쓸 자리가 없어진다.**
   한 줄로 두고 옆으로 밀어 보게 했다(휴대폰 편집기들이 하는 방식).
3. iOS 는 글씨가 16px 보다 작은 입력칸을 누르면 **화면을 통째로 확대한다.** 입력칸
   글씨를 16px 아래로 내리지 않는 것으로 막았다. `user-scalable=no` 는 쓰지 않았다 —
   손가락으로 넓혀 보는 것까지 빼앗을 이유가 없다.

그 밖에 편집기 머리의 글자 이름표를 걷어 아이콘만 남기고(가장 자주 누르는 *저장* 만
글자로 둔다), 카드는 한 칸씩 세우고, 표는 본문을 밀지 않고 표만 따로 밀리게 했다.
375×812 에서 새 메모 쓰기 → 저절로 저장 → 목록 → 검색 → 사진 크기 바꾸기까지
하나씩 눌러 확인했다.

### Jaden wiki — 포털 안 메모, 완료

로드맵 맨 아래에 "네이버 메모 자체 제작"으로 적어 두었던 것을 만들었다. 네이버 메모와
같은 일을 하되, 메모가 우리 D1에 있으니 목록·검색·미리보기가 전부 우리 것이다.

- **대시보드**(`#/wiki`) — 왼쪽에 폴더(전체·중요·내 메모·휴지통), 가운데에 메모 카드.
  검색(제목·본문·#태그를 함께 훑고 걸린 자리에 표를 한다), 정렬 네 가지, 카드/목록 보기.
- **편집기**(`#/wiki/<번호>`) — MS Teams 작성칸에 있는 것들을 같은 순서로 놓았다.
  문단 모양·글자 크기·굵게·기울임·밑줄·취소선·글자색·형광펜·글머리·번호·체크리스트·
  들여쓰기·정렬·인용·코드·구분선·표·링크·사진·서식 지우기. 2초마다 저절로 저장하고
  `Ctrl+S` 로 바로 저장한다.
- **사진** — 단추·붙여넣기·끌어다 놓기 셋 다. R2(`japis-wiki`)에 들어가고 본문에는
  `/api/wiki/files/<id>` 만 적힌다(로그인해야 열리고, 남의 칸은 잡히지 않는다).
- **html 저장/불러오기** — 내보낼 때 사진을 `data:` 로 심어 파일 하나로 들고 나갈 수 있다.
  불러오기는 목록에서 여러 개(파일마다 새 메모), 편집기에서 하나(지금 메모에 덧붙이기).
- **본문은 서버가 씻어서 저장한다**(`sanitizeHtml`). 편집기가 만든 것도, 받은 파일도
  같은 문을 지난다.

옛 '제이든 wiki'(옵시디언 `obsidian://open?vault=Jaden`)는 이 화면이 대신한다. 그 주소는
이 컴퓨터의 프로그램을 부르는 것이라 휴대폰에서도 남의 컴퓨터에서도 열리지 않았다.
권한 행이 붙어 있는 `jadenwiki` key 는 그대로 두었다.

> 편집기를 만들며 한 번 밟은 곳 — 글자 크기를 `styleWithCSS=false` 로 넣으면 브라우저가
> 옛 `<font size>` 태그를 만드는데, 그 태그는 씻어 내는 쪽에서 사라진다. 화면에서는
> 커졌다가 저장하고 돌아오면 되돌아가 있는, 가장 알아채기 어려운 고장이 된다.
> 들여쓰기도 같은 얼개였다(`margin: 0 0 0 40px` 줄임표기를 허용 목록에 넣지 않았다).
> 서식 하나하나를 저장했다 다시 읽어 보고서야 둘 다 잡혔다.

### 왼쪽 기둥 · 상단 '지금' · 오른쪽 프레임 — 완료

- 메뉴를 상단 가로줄에서 **왼쪽 기둥**으로. 묶음마다 개수, 한 줄은 `[아이콘] 이름`,
  손이 닿으면 `↗`. 비워진 상단 가운데에는 **날짜·시간·날씨**(`/api/weather`).
- 대시보드 맨 위 **'오늘'** — 다가오는 일정·안 읽은 메일을 기본으로 펼쳐 둔다.
- **오른쪽 프레임** — 이름·카드를 누르면 포털을 떠나지 않고 그 자리에서 연다.
  `↗` 는 새 탭. 프레임을 막아 둔 사이트는 눌러도 곧바로 새 탭이다(`/api/frameable`
  이 헤더를 보고 미리 판단해 KV에 하루 담아 둔다).
- **재인증(화면 열 때 비밀번호 재확인)을 껐다.** 이미 로그인해 들어온 사람에게 링크
  하나 누를 때마다 또 묻는 것은, 보태는 안전보다 깎아먹는 쓸모가 컸다. 장치는 남아
  있다 — 관리 → 화면 권한에서 화면별로 다시 켤 수 있다.
- 구글 메일·캘린더 주소를 `?authuser=<이메일>` 형식으로. `/u/<이메일>/` 의 `u/` 자리를
  구글이 계정 번호로 읽어 "요청한 URL을 찾을 수 없습니다"가 뜨고 있었다.

### 구글 링크에 계정 박기 — 완료

"구글 포토가 안 열린다"의 정체를 찾았다. 주소를 끝까지 따라가 보니

    photos.google.com/?authuser=garzette@gmail.com
      → (authuser 를 지우고) photos.google.com/
      → www.google.com/photos/about/          ← 소개 페이지

포토는 `authuser` 를 **버린다.** 캘린더는 지키고, 메일은 로그인 단계에서 조용히
버린다 — 서비스마다 제각각이다. 그래서 넷 모두 `googleAs()` 로 감쌌다
(`accounts.google.com/AccountChooser?Email=…&continue=…`). 이미 그 계정으로 로그인해
있으면 그대로 통과하고, 여러 계정이 물려 있으면 이 계정으로 바꿔 주고, 로그인 전이면
이메일이 채워진 로그인 화면이 뜬다. 어느 쪽이든 엉뚱한 곳에 닿지 않는다.

프레임에 넣는 주소(frameUrl)는 그대로 두었다 — accounts.google.com 은 프레임을 막는다.

### 대시보드 정리 · 구글 할일 — 완료

- **대시보드를 한 화면에.** 설명이 붙은 카드를 이름만 남긴 타일로 바꾸고, 머리말
  ("흩어져 있던 서비스를…")을 지웠다. 대시보드는 둘러보는 곳이 아니라 고르는 곳이다.
  설명은 묶음 화면의 카드가 그대로 맡는다.
- **구글 할일**을 협업에 더하고, 대시보드 '오늘'에 *남은 할 일* 칸을 얹었다
  (기한이 있는 것부터, 끝낸 것은 빼고 다섯 개).
- '오늘'이 셋이 되어 제목 자체를 여는 단추로 바꿨다 — 옆에 '열기'를 또 두면 세 칸이
  나란히 설 자리가 없다.
- 스코프에 `tasks.readonly` 가 늘었다. 옛 토큰으로 부르면 403이 오는데, 이 경우를
  알아보고 카드에 *구글 다시 연결하기* 를 띄운다(예전에는 "불러오지 못했습니다"로
  뭉뚱그려져 무엇을 해야 하는지 알 수 없었다).

### 프레임 손질 — 완료

14개 화면을 하나씩 눌러 확인했다. 기능은 처음부터 정상이었고, 문제는 두 가지였다.

1. **프레임에 담기는 것이 4개뿐**이라 "안 먹는 것"처럼 보였다.
   → 구글 캘린더에 `frameUrl`(공식 임베드 주소)을 붙여 5개로. 본 화면은
     `X-Frame-Options: SAMEORIGIN` 이지만 임베드는 붙여 쓰라고 만든 것이라 헤더가 없다.
2. **`↗` 가 마우스를 올려야만 보였다.** → 카드의 ↗ 는 늘 보이게, 기둥에서는
   **새 탭으로만 열리는 줄**의 ↗ 를 늘 보이게(그 줄의 유일한 길이므로).
   카드 태그도 `바로 열기` → `▸ 오른쪽에서` / `↗ 새 탭` 으로 바꿔, 누르기 전에 알 수 있게.

### 남은 것

- **카드모아**가 `frame-ancestors 'none'` · `X-Frame-Options: DENY` 를 내보내 프레임에
  담기지 않는다. 그 저장소에서 JAPIS 주소를 허용하면 프레임에서 열린다.
- 구글 포토·메일, 네이버 메모, OneDrive 는 임베드 주소 자체가 없어 새 탭이 끝이다.
  메일·일정의 '내용'은 대시보드 '오늘'이 대신 보여준다.

## 반영 완료 (2026-09-12)

### 1. 모바일 생체인증 — 완료

- 대상: **최초 접속(로그인)** · **화면 이동 시 재인증(잠금해제)** 두 곳 모두.
- WebAuthn 패스키로 붙였다. 비밀번호를 대신하는 것이 아니라 나란히 둔 두 번째 길이다.
- `userVerification: 'required'` — "기기를 갖고 있다"가 아니라 "생체인증을 통과했다"만
  통과한다. 열쇠는 rpId(호스트)에 매이고, 서버에는 공개키만 남는다.
- 등록은 **내 계정 → 생체인증 → 이 기기 등록** (현재 비밀번호를 한 번 더 받는다).
- 기기에 지문·얼굴 잠금이 없으면 버튼 자체를 띄우지 않는다.

### 2. 메뉴 재구성 — 완료

`service`/`tool`/`connect` 3분류 → **개인서비스 / 홈페이지 / 협업**.

| 묶음 | 화면 |
|---|---|
| 개인서비스 | wepic · 가계부 · 카드모아 · Taylor Bookshelf · 타임라인 · 제이든 wiki |
| 홈페이지 | Word Writer · Julie · SNS |
| 협업 | 구글 포토 · 구글 메일 · 구글 캘린더 · 네이버 메모 · One Drive |

- "구글 타임라인" → **타임라인** (key `timeline` 은 그대로 — 권한표가 그 값으로 붙어 있다)
- **제이든 wiki** 신설 — 옵시디언(`obsidian://open?vault=Jaden`)으로 연결
- **SNS** 신설 — 포털 안 화면 하나에 인스타그램·Thread·Facebook·Telegram
- **삭제**: 온라인 갤러리 · 개인 위키 · 기업정보 에이전트 (권한 행은 `migrations/001` 이 정리)

### 3. 협업 카드에 최근 데이터 — 얼개 완료, 열쇠 대기

카드 앞면에 최근 것 몇 개가 올라온다(안 읽은 메일 제목 · 다가오는 일정 · 최근 사진
썸네일 · 최근에 연 문서). Worker가 API 프록시가 되고 브라우저는 토큰을 만지지 않는다.

- 구글 포토·메일·캘린더는 **연결 하나를 나눠 쓴다** — 한 번 동의하면 셋 다 열린다.
- Refresh Token 은 AES-GCM 으로 싸서 D1에 둔다. 피드는 5분 캐시.
- 잠긴 화면은 **잠금을 푼 뒤에만** 내용이 보인다.
- 네이버 메모는 API가 없어 지금은 링크(memo.naver.com)만 — 자체 제작이 그 자리다.

---

## 남은 일

### 곧 해야 하는 것 (사람 손이 필요)

| 할 일 | 어디를 고치나 |
|---|---|
| 구글·마이크로소프트 OAuth 클라이언트 등록 후 시크릿 4개 넣기 | Cloudflare 대시보드 — [cloudflare/README.md](cloudflare/README.md) 3번 |
| **구글 연결 한 번 끊었다 다시 하기** (쓰기 권한이 늘었다 — 한 줄 입력칸이 쓴다) | 협업 화면의 '연결' 판 |
| `006_bookmarks.sql` 을 운영 D1 에 적용 | `npx wrangler d1 execute japis-db --remote --file=./migrations/006_bookmarks.sql` |
| `007_playlists.sql` 을 운영 D1 에 적용 | `npx wrangler d1 execute japis-db --remote --file=./migrations/007_playlists.sql` |
| SNS 6곳을 본인 계정·채널 주소로 | [cloudflare/services.js](cloudflare/services.js) `sns.links` |
| Word Writer 배포 후 **Prompt Builder 주소** 적기 | [cloudflare/services.js](cloudflare/services.js) `wordwriter.url` |
| 타임라인 배포 후 주소 적기 | 완료 — `https://timeline.wepiclab.workers.dev/` |

### 그다음

- **휴대폰 알림을 텔레그램 한 곳으로 모으기.**
  알림이 앱마다 흩어져 있으면 결국 아무것도 보지 않게 된다. 포털이 아는 것
  (안 읽은 메일, 오늘 일정, 기한이 지난 할 일)을 **정해진 시각에 한 덩어리로 묶어**
  텔레그램 봇이 한 줄씩 보내 준다.
  - 얼개: Worker 의 `scheduled` 트리거(이미 접속기록 청소에 쓰고 있다)에 붙인다 →
    `feedFor()` 로 세 가지를 모아 하나의 글로 엮고 → Telegram Bot API
    (`sendMessage`) 로 민다. 새로 필요한 것은 봇 토큰과 채팅 번호 둘뿐이다
    (`TELEGRAM_BOT_TOKEN` · `TELEGRAM_CHAT_ID` — 시크릿).
  - 정할 것: 보내는 시각(아침 7시?), 조용한 시간, "바뀐 것만 보낼지" 아니면
    "매번 전부 보낼지". 같은 말을 두 번 보내면 그 다음부터 아무도 읽지 않는다.
  - 먼저 할 일: 텔레그램에서 봇 하나 만들기(@BotFather) → 그 봇에게 말을 걸어
    chat id 알아내기.

- **SNS 정보 요약 — 요약해서 텔레그램으로.**
  인스타그램·Thread·Facebook·LinkedIn·다모앙을 하나씩 돌아보는 시간이 아깝다.
  새로 올라온 것을 모아 **몇 줄로 줄여** 위와 같은 길(텔레그램)로 보낸다.
  - 걸림돌은 기술이 아니라 **문**이다. 메타(인스타·Thread·Facebook)는 남의 피드를
    API로 내주지 않는다 — 내 계정의 내 글은 되고, 남의 타임라인은 안 된다.
    LinkedIn 도 마찬가지다. 다모앙은 RSS 가 있으면 그것이 가장 깨끗한 길이다.
  - 그래서 순서를 이렇게 잡는다: ① RSS·공개 피드가 있는 곳부터(다모앙·블로그)
    → ② 내 계정의 알림(멘션·댓글)을 API 로 → ③ 나머지는 '링크만 모아 주기'.
  - 요약은 Workers AI(`@cf/meta/llama-*`)로 그 자리에서 돌린다. 한 줄 입력칸과 달리
    여기서는 **줄이는 일**이 본업이라 규칙으로는 되지 않는다.

- **검색(3단계)** — Cloudflare Vectorize 로 메모·문서·사진 캡션을 한 번에.
  협업 데이터가 모이고 난 뒤의 이야기다.
